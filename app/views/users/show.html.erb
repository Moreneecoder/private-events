<p><%= flash[:notice] %></p>
<h1>Hey, <%= @user[:name] %>!</h1>

<table>
    <thead>
        <strong>YOUR EVENTS</strong>
    </thead>
    <tbody>
        <tr>
            <th>Title</th>
            <th>Location</th>
            <th colspan="3">Date</th>
        </tr>

        <% @user_events.each do |event| %>
            <tr>
                <td><%= event.title %></td>
                <td><%= event.location %></td>
                <td><%= event.date %></td>
            </tr>
        <% end %>
    </tbody>
</table>

<a href="#">Create Event</a> | <%= link_to 'logout', users_logout_path%>
